import React from 'react'

export default function App(){
  return (
    <div style={{fontFamily:'Arial, sans-serif',padding:20}}>
      <h1>Diag Plomb — Demo</h1>
      <p>Application PWA React (squelette). Déployée sur GitHub Pages pour accès depuis iPad.</p>
    </div>
  )
}
